name: schema
description: Base schema definitions for Kabuk Project
version: 0.0.1

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  sqlite3: ^2.7.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
